#include<bits/stdc++.h>

using namespace std;

int solve(int arr[], int dep[], int n)
{
    
}

int main()
{
    int n;
    cin>>n;
    int arr[n], dep[n];
    for(int i=0; i<n; i++)
        cin>>arr[n];
    for(int i=0; i<n; i++)
        cin>>dep[n];
    solve(arr, dep, n);
    return 0;
}